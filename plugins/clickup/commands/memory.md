---
argument-hint: "[add | list | remove | clear]"
description: "Manage learned ClickUp memory rules in ~/.claude/clickup/memory.md — add, list, remove, or clear patterns."
---

Invoke the `clickup:clickup` skill via the Skill tool, passing `--memory $ARGUMENTS`. The skill's SKILL.md routes to `references/modes.md#memory` with the sub-arg (`add|list|remove|clear`) passed through.
