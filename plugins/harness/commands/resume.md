---
description: "Resume the current run after a crash, reboot, or stop: restart the watch loop and respawn dead sessions with their exact recorded session ids."
---

Invoke the `harness:harness` skill via the Skill tool, passing `--resume $ARGUMENTS` through verbatim. The skill's flag router handles the `--resume` mode.
