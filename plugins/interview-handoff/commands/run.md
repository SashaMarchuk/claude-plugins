---
argument-hint: "[topic]"
description: "/interview-handoff:run — grill until the design tree is exhausted, then write the handoff package (questions, context, prompt) for an interviewer to run elsewhere."
---

Invoke the `interview-handoff:interview-handoff` skill via the Skill tool, passing `--run $ARGUMENTS`. The skill runs its `## Mode: run` flow. This command holds no workflow of its own.
