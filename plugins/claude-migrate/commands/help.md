---
argument-hint: ""
description: "(beta) Show the claude-migrate command guide: the onboarding flow, every command, and per-step recovery. Use when the user types /claude-migrate:help, or says \"how do I use claude-migrate\", \"migration help\", \"what are the migration commands\"."
---

Invoke the `claude-migrate:help` skill via the Skill tool, passing `$ARGUMENTS` through verbatim. The skill prints the command guide, the recommended onboarding sequence, and what to do when a run blocks at each step.
