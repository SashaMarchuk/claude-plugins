---
description: "Show the active run: sessions, heartbeat ages, rate limits, attention items, ticket queue."
---

Invoke the `harness:harness` skill via the Skill tool, passing `--status $ARGUMENTS` through verbatim. The skill's flag router handles the `--status` mode.
