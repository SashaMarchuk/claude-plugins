---
argument-hint: ""
description: "Health-check both gevent config files (~/.claude/shared/identity.json and ~/.claude/gevent/config.json) plus Google Workspace CLI auth. Read-only."
---

Invoke the `gevent:gevent` skill via the Skill tool, passing `--status $ARGUMENTS`. The skill's SKILL.md routes to `references/modes.md#status` — never mutates state.
