---
argument-hint: ""
description: "Switch the active default Google Calendar. Updates `defaults.calendar` in `~/.claude/g-event/config.json`."
---

Invoke the `g-event:g-event` skill via the Skill tool, passing `--calendar $ARGUMENTS`. The skill's SKILL.md routes to `references/modes.md#calendar` and writes the new default calendar atomically.
