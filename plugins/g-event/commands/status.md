---
argument-hint: ""
description: "Health-check both g-event config files (~/.claude/shared/identity.json and ~/.claude/g-event/config.json) plus Google Workspace CLI auth. Read-only."
---

Invoke the `g-event:g-event` skill via the Skill tool, passing `--status $ARGUMENTS`. The skill's SKILL.md routes to `references/modes.md#status` — never mutates state.
